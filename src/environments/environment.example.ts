/**
 * TEMPLATE — copy this to environment.ts and fill in real keys.
 * environment.ts is gitignored — never commit real keys.
 *
 * How to get each key:
 *   groqApiKey      → https://console.groq.com/keys
 *   geminiApiKeys   → https://aistudio.google.com/app/apikey
 *   youtubeApiKeys  → https://console.cloud.google.com/apis/credentials
 *                     (enable "YouTube Data API v3" first)
 */
export const environment = {
  production: false,
  groqApiKey: '',
  geminiApiKeys: [''],
  youtubeApiKeys: [''],
};
